# Encrypt Decrypt
Small example to demo encrypting and decrypting a raw string in a flat file.

## Requirements
* Node
* Grunt

## Setup
First thing you want to do is install all node packages run:

    npm install
    
To generate the encrypted flat file run something like:

    node saveemailcreds test@test.com somePassword

To lint and test run:

    grunt test